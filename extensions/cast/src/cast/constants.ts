export const CAST_TOPIC_SESSION_KEY = 'ohif.cast.sessionTopic';
export const CAST_PRODUCT_NAME = 'OHIF';
export const LOG_PREFIX = 'CastService(OHIF):';

/** Wire ``target.product.name`` for TotalSegmentator Cast subscribers. */
export const TOTAL_SEGMENTATOR_PRODUCT_NAME = 'TOTALSEG';

/** OHIF data source for Cast IDC direct bucket download (not DICOMweb). */
export const CAST_IDC_DATA_SOURCE = 'idc';

/** OHIF data source for Cast-ingested local DICOM (dicom-send, zip, etc.). */
export const CAST_LOCAL_DATA_SOURCE = 'dicomlocal';
